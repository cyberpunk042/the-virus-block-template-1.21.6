# Field Transforms

Place JSON files here for `$ref` resolution.

## Usage

```json
{
  "$ref": "$field_transforms/my_preset"
}
```

This will load `config/the-virus-block/field_transforms/my_preset.json`
