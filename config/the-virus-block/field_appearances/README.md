# Field Appearances

Place JSON files here for `$ref` resolution.

## Usage

```json
{
  "$ref": "$field_appearances/my_preset"
}
```

This will load `config/the-virus-block/field_appearances/my_preset.json`
