# Field Animations

Place JSON files here for `$ref` resolution.

## Usage

```json
{
  "$ref": "$field_animations/my_preset"
}
```

This will load `config/the-virus-block/field_animations/my_preset.json`
