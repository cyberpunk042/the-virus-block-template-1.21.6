# Field Arrangements

Place JSON files here for `$ref` resolution.

## Usage

```json
{
  "$ref": "$field_arrangements/my_preset"
}
```

This will load `config/the-virus-block/field_arrangements/my_preset.json`
